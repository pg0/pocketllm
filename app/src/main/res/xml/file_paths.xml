<?xml version="1.0" encoding="utf-8"?>
<!--
    The one path the camera app is allowed to write into: the same cache
    directory every other attachment is staged in. Nothing else is shared.
-->
<paths>
    <cache-path name="attachments" path="attachments/" />
</paths>
